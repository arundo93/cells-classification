# **Содержание**

1. [Локальное развертывание без Docker и Kafka](#локальное-развертывание-без-docker-и-kafka)

   - [Общие требования](#общие-требования)
   - [Подготовка виртуального окружения Python](#1-подготовка-виртуального-окружения-python)
   - [Сборка приложения клиента](#2-сборка-приложения-клиента)
   - [Запуск сервера](#3-запуск-сервера)
   - [Дополнительные замечания](#дополнительные-замечания)
     - [Проверка зависимостей](#1-проверка-зависимостей)
     - [Запуск в dev mode (разработка)](#2-запуск-в-dev-mode-разработка)

2. [Развертывание в Docker контейнере](#развертывание-в-docker-контейнере)
   - [Общие требования](#общие-требования-1)
     - [Проверка установки](#проверка-установки)
     - [Установка Docker и Docker Compose](#🔧-установка-docker-и-docker-compose)
   - [Основные команды](#🛠️-основные-команды)
     - [Сборка образов](#🧱-сборка-образов)
     - [Запуск всех сервисов](#▶️-запуск-всех-сервисов)
     - [Пересборка без кэша](#🔁-пересборка-без-кэша)
     - [Пересборка только одного сервиса](#🔨-пересборка-только-одного-сервиса)
     - [Состояние контейнеров](#📋-состояние-контейнеров)
     - [Логи сервиса](#📄-логи-сервиса)
     - [Вход внутрь контейнера](#🕵️-вход-внутрь-контейнера)
     - [Очистка](#🗑️-очистка)

---

# **Локальное развертывание без docker и kafka**

## **Общие требования**

Перед началом работы убедитесь, что:

1. Установлен Python 3.x (проверьте командой `python --version` или `python3 --version`).
2. В директории сервиса `ai-inference` есть файл `requirements.txt` с перечислением всех зависимостей.
3. Установлена Node.JS с npm (проверить можно командами `node -v` и `npm -v`)

---

## **Подготовка окружения и запуск приложения**

### **1 Подготовка виртуального окружения Python**

**Для Linux/macOS**

1. Создание виртуального окружения python.

Команды выполняются в директории `ai-inference`

```bash
python3 -m venv .py3_venv
```

2. Активация виртуальной среды

```bash
source .py3_venv/bin/activate
```

3. Проверка активации вируального окружения

Об этом может свидетельствовать имя `(.py3_venv)` в приглашении командной строки, например, `(.py3_venv) ~/path/to/your/project`. Также проверку можно выполнить с помощью команды

```bash
which python
```

В результате должен быть выведен путь, соответствующий расположению интерпретатора в текущем окружении `(.py3_venv)`.

4. Обновление pip (необязательно)

```bash
pip install --upgrade pip
```

5. Установка зависимостей

[info] Перед установкой зависимостей нужно убедиться, что вируальное окружение активировано.

> Проверить список пакетов, установленных в текущем окружении можно командой

> ```bash
> pip list
> ```

> Если окружение только создано, в выводе будет пустой список пакетов

```bash
pip install -r requirements.txt
```

---

**Развертывание на Windows через CMD**

1. Создание виртуальной среды. Команды выполняются в директории `ai-inference`.

[info] Если у вас не установлен `python3`, а есть `python`, используйте его

```bash
python3 -m venv .py3_venv
```

2. Активация виртуальной среды

Для активации виртуальной среды выполните одну из следующих команд в зависимости от используемой оболочки:

**Для Command Prompt (CMD):**

```bash
call .\.py3_venv\Scripts\activate.bat
```

> Примечание: Команда `call` используется для запуска скрипта активации внутри текущей сессии. Если вы получаете ошибку, попробуйте просто:
>
> ```bash
> .\.py3_venv\Scripts\activate.bat
> ```

**Для PowerShell:**

```powershell
.\.py3_venv\Scripts\Activate.ps1
```

**Для Git Bash (или других Unix-подобных оболочек на Windows):**

```bash
source ./.py3_venv/Scripts/activate
```

> Примечание: Убедитесь, что вы используете правильный путь к файлу `activate`.
> В некоторых случаях может потребоваться заменить обратные слеши (`\`) на прямые (`/`).

3. Убедиться, что вируальное окружение активировано

Об этом может свидетельствовать имя `(.py3_venv)` в приглашении командной строки, например, `(.py3_venv) C:\path\to\your\project`. Также проверку можно выполнить с помощью команды

```bash
which python
```

В результате должен быть выведен путь, соответствующий расположению интерпретатора в текущем окружении `(.py3_venv)`.

4. Обновление pip (необязательно)

```bash
pip install --upgrade pip
```

5. Установка зависимостей

[info] Перед установкой зависимостей обязательно проверить, что вируальное окружение активировано.

> Проверить список пакетов, установленных в текущем окружении можно командой

> ```bash
> pip list
> ```

> Если окружение только создано, в выводе будет пустой список пакетов

```bash
pip install -r requirements.txt
```

### **2 Сборка приложения клиента**

Запуск сборки осуществляется из директории `client`:

```bash
npm install
npm run build
```

> Важно убедится, что в результате запуска команды `npm run build` сборка проходит без ошибок и отображается расположение файлов, полученных в результате сборки

### **3 Запуск сервера**

Запуск Python-сервера `uvicorn main:app`

Запуск сервера Node.js осуществляется из директории `nodejs`:

```bash
npm install
npm run run
```

Для проверки python сервера можно использовать команды

1. Проверка сервера (health_check)

```bash
curl http://localhost:8000/health
```

2. Отправка задачи

```bash
curl -X POST http://localhost:8000/task/create \
    -H "Content-Type: application/json" \
    -d '{"filePaths": ["path/to.jpg"], "models": ["VisionTransformer"]}'
```

---

## **Дополнительные замечания**

### **1 Проверка зависимостей**

Если вы добавляете новые зависимости в проект, обновите файл `requirements.txt`:

```bash
pip freeze > requirements.txt
```

> Выполенние команды необходимо производить при активированном виртуальном окружении (см. раздел про активацию виртуального окружения python)

### **2 Запуск в dev mode (разработка)**

Режим разработки (`dev mode`) предназначен для удобной и быстрой разработки приложения. Он включает в себя автоматическую перезагрузку при изменении кода (HMR — Hot Module Replacement), подробное логирование, а также дополнительные возможности отладки.

**Возможности**

- **Горячая замена модулей (HMR)**: изменения в компонентах мгновенно отображаются в браузере без полной перезагрузки страницы.
- **Live reload**: при изменении структуры или стилей, страница автоматически перезагружается.
- **Интерфейс отладки**: вывод ошибок и предупреждений прямо в браузер.
- **Локальный сервер разработки**: запускается на `http://localhost:3000` (или другом порту, если занят).
- **Предзаполненные переменные окружения**: `.env.development` применяется автоматически.

**Особенности**

- Не предназначен для production.
- Использует неоптимизированные сборки, которые медленнее и больше по размеру.
- Активированы инструменты разработки (например, React Developer Tools).

Запуск приложения клиента в dev mode с hmr осуществляется из директории `client` командой

```bash
npm run dev
```

Запуск приложения сервера в dev mode с hmr осуществляется из директории `nodejs` командой

```bash
npm run dev
```

> Выполенние команды для сервера необходимо производить при активированном виртуальном окружении (см. раздел про активацию виртуального окружения python)

---

# **Развертывание в Docker контейнере**

## **Общие требования**

- ✅ [Docker](https://docs.docker.com/get-docker/)
- ✅ [Docker Compose](https://docs.docker.com/compose/install/)

### Проверка установки

```bash
docker --version
docker-compose --version
```

### 🔧 Установка Docker и Docker Compose

**🐧 Linux (Ubuntu/Debian)**

```bash
# Установка Docker
sudo apt update && sudo apt install docker.io -y

# Установка Docker Compose
sudo apt install docker-compose -y
```

**🍎 macOS**

Установите [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop)

> Включает Docker + Docker Compose

**💻 Windows**

1. Установите [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop)
2. Активируйте WSL2 (Windows Subsystem for Linux), если предложит инсталлятор

---

## 🛠️ Основные команды

### 1. 🧱 Сборка образов

```bash
docker-compose build
```

При необходимости выдачи прав пользователю не рекомендуется использовать `sudo`, вместо этого лучше добавить пользователя в гркппу:

```bash
sudo usermod -aG docker $USER
```

> Используется при первом запуске или после изменения кода

---

### 2. ▶️ Запуск всех сервисов

```bash
docker-compose up -d
```

> `-d` — запуск в фоне

Спопсобы запуска и остановки:

| Задача                                     | Команда                            |
| ------------------------------------------ | ---------------------------------- |
| Приостановить работу                       | `docker-compose stop`              |
| Возобновить                                | `docker-compose start`             |
| Полная очистка                             | `docker-compose down`              |
| Запуск после остановки                     | `docker-compose up`                |
| Приостановить, но не выключать процессы    | `docker-compose pause` / `unpause` |
| Сохранить образ для будущего использования | `docker save` / `load`             |

---

### 3. 🔁 Пересборка без кэша (чистая сборка)

Полезно при обновлении зависимостей:

```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

### 4. 🔨 Пересборка только одного сервиса

Например, только `service1`:

```bash
docker-compose build --no-cache service1
docker-compose up -d --force-recreate service1
```

> Использование `--no-cache` не является обязательным и необходимо для сборки контейнера заново

---

### 5. 📋 Состояние контейнеров

```bash
docker-compose ps
```

Все контейнеры:

```bash
docker ps
```

---

### 6. 📄 Логи сервиса

```bash
docker-compose logs <service-name>
```

С реальным временем (для примера сервис nodejs):

```bash
docker-compose logs -f nodejs
```

---

### 7. 🕵️ Вход внутрь контейнера

Для отладки или проверки состояния:

```bash
docker exec -it <container_name> sh
```

Имя контейнера можно узнать в выводе:

```bash
docker-compose ps
```

---

### 8. 🗑️ Очистка

❌ Остановить и удалить контейнеры

```bash
docker-compose down
```

🚫 Полная очистка: образы + volume'ы

```bash
docker-compose down --volumes --rmi all
```

🗑️ Полная очистка системы (включая volumes, сети и т.д.)

```bash
docker system prune -af --volumes
```

> ⚠️ Осторожно! Это полностью очищает систему.

---

🧹 Ручная очистка (по желанию)

| Команда                  | Что делает                                    |
| ------------------------ | --------------------------------------------- |
| `docker system prune -a` | Чистка неиспользуемых образов, сетей, volumes |
| `docker images`          | Посмотреть список образов                     |
| `docker rmi <image_id>`  | Удалить конкретный образ                      |
| `docker volume ls`       | Посмотреть volume'ы                           |
| `docker volume prune`    | Очистить неиспользуемые volume'ы              |

---
